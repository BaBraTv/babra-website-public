export default async function ResetPasswordPage({ searchParams }: { searchParams: Promise<{ token?: string }> }) { const { token = "" } = await searchParams; return <main className="mx-auto max-w-md px-6 py-16"><h1 className="text-3xl font-bold">Choose a new password</h1><form action="/api/academy/v1/auth/reset-password" method="post" className="mt-8 space-y-5"><input type="hidden" name="token" value={token} /><label className="block">New password<input name="password" type="password" minLength={12} required className="mt-2 w-full rounded-lg border border-slate-700 bg-slate-900 px-4 py-3" /></label><button className="w-full rounded-lg bg-amber-300 px-4 py-3 font-semibold text-slate-950">Update password</button></form></main>; }
